import React from "react";
import { Request } from "express";

export const ErrorResponse = () => {
    // const [path] = React.useState[0];
    return (
        <h1> Unable to locate page </h1>
    );
}; 